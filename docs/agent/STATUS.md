# Текущее состояние

Снимок на 2026-09-10. Этот файл заменяет предыдущий снимок и не является
журналом.

## Подтверждено

- Создана минимальная система проектных инструкций и документации.
- Первый челлендж реализован в `challenges/day-01-llm-api` как локальное
  веб-приложение на Node.js, React и Vinext.
- Серверный маршрут вызывает OpenAI Responses API. Ключ берётся из
  `OPENAI_API_KEY`, передаётся только серверному runtime и не попадает в
  браузерный код.
- Интерфейс позволяет менять модель, инструкцию, запрос, температуру и лимит
  ответа; после запроса показывает текст, токены и ориентировочную стоимость.
- Выбор интерфейса и рассмотренные альтернативы зафиксированы в
  [решении 0001](../decisions/0001-day-01-local-web.md).
- Production-сборка, линтер и локальная HTTP-проверка прошли. Реальный запрос
  к `gpt-5.6-luna` вернул ответ и usage-метрики.
- Отрицательный тест без ключа вернул `503`; проверка итоговых генерируемых
  файлов не нашла точного значения ключа в `dist`, `.vinext` или `.wrangler`.
- После обновления и очистки зависимостей `npm audit` сообщает 0 известных
  уязвимостей.
- Локальные файлы IntelliJ IDEA сохранены на диске, исключены через корневой
  `.gitignore` и удалены из Git index перед первым коммитом.
- Второй челлендж реализован как компактное Kotlin/JVM + Compose Desktop
  приложение в `challenges/day-02-response-control`: два ответа рядом,
  параллельный streaming, ограничения и проверки результата.
- Условия сравнения описаны в [задании 2](../features/day-02-response-control.md),
  выбор реализации — в [решении 0002](../decisions/0002-day-02-kotlin-desktop.md).
- Подготовлена [справочная статья](../guides/response-control.md) с примерами
  Kotlin и ссылками на официальную документацию.
- Для второго задания прошли `test assemble` и 12 тестов без платных вызовов,
  включая два локальных SSE-потока, отмену соединения и настраиваемые ограничения.
- Модель выбирается из списка; ограничения справа задаются списками и
  ползунками. Оценка ответа использует снимок настроек выполненного запуска.
- Обновлённое окно запущено с обычным окружением; индикатор подтвердил наличие
  ключа в процессе без проверки его действительности в API. Две колонки и
  регуляторы визуально проверены. Дальнейшая проверка кликов прервана
  пользователем через Escape; результат клика по списку модели не подтверждён.
- Третий челлендж реализован в `challenges/day-03-reasoning-comparison` как
  локальная веб-лаборатория: фиксированная задача, четыре стратегии промптинга,
  пять API-вызовов, локальная проверка эталона и Markdown-экспорт.
- Условия сравнения описаны в [задании 3](../features/day-03-reasoning-comparison.md),
  выбор реализации — в [решении 0003](../decisions/0003-day-03-local-web.md).
- Для третьего задания прошли 6 локальных тестов, линтер и production-сборка;
  реальные платные вызовы в ходе реализации не выполнялись.
- 2026-09-05 по подтверждению пользователя выполнен реальный эксперимент
  третьего задания: пять API-вызовов к `gpt-5.6-luna`. Все четыре стратегии
  завершились. Прямой ответ — `14`, пошаговый — `7`, метапромпт — `10`,
  каждый из трёх экспертов — `10`. По численному итогу верны два из четырёх
  способов; первое место разделили метапромпт и группа экспертов.
- У критика в группе экспертов обнаружены ошибочные промежуточные вычисления
  при верном конечном ответе. Результат подтверждает ограничение численной
  оценки и относится только к этому запуску.
- Markdown-отчёт и JSON результатов сохранены локально вне репозитория.
  Использовано 1016 входных и 2512 выходных токенов. Повторная проверка
  шести локальных тестов прошла; локальная главная страница вернула HTTP 200.
- Четвёртый челлендж выполнен в `challenges/day-04-temperature` как Node.js-скрипт
  и локальная React/Vinext-лаборатория в `web` с двумя сценариями. Условия — в [задании 4](../features/day-04-temperature.md),
  принятое решение — в [решении 0004](../decisions/0004-day-04-script.md).
- По подтверждению пользователя 2026-09-05 выполнены девять реальных API-вызовов
  к `gpt-5.6-luna`: по три на `temperature = 0`, `0.7`, `1.2`. Все завершились.
  JSON и Markdown-протокол сохранены в каталоге задания; описание, справка,
  сравнение и итоговые выводы — в [README](../../challenges/day-04-temperature/README.md).
- По дополнительному подтверждению пользователя 2026-09-05 выполнены ещё девять
  вызовов для мастерской снов. Все завершились; обе серии (18 ответов) сохранены
  в задании и доступны в веб-интерфейсе без API. Новые веб-запуски используют
  общий с CLI исполнитель; ответы появляются по мере получения и экспортируются.
- Для Дня 4 прошли 14 тестов Node.js и один интеграционный тест workerd,
  линтер, проверка типов, production-сборка
  и локальные HTTP-проверки. Анализ обеих серий и ограничения находятся в README.
  Взаимодействия в браузере и необязательный WebMCP read-инструмент не проверялись.
- Исправлена несовместимость `redirect: error` с установленным workerd, которая
  останавливала веб-запуск до первого запроса. Режим `manual` не следует
  редиректам; регрессионный тест проверяет полный поток без платного API.
  В интерфейсе явно различаются сохранённый эксперимент и текущий запуск.
- Пятый челлендж реализован в `challenges/day-05-model-comparison`: локальная
  React/Vinext-лаборатория на порту 3005, три колонки моделей, выбор повтора,
  метрики, проверка окончательного плана и сумма баллов за три повтора.
  Условия — в [задании 5](../features/day-05-model-comparison.md), выбор — в
  [решении 0005](../decisions/0005-day-05-local-web.md).
- По подтверждению пользователя 2026-09-05 выполнены девять реальных вызовов:
  по три к Luna, Terra и Sol, одинаковый запрос про научный фестиваль,
  температура 0 и reasoning none. Все завершены, расход — 3060 входных и
  2984 выходных токена, оценочная стоимость $0,0384128 при бюджете $0,20.
- По окончательным ответам оптимум найден у Luna 0/3, Terra 1/3, Sol 3/3.
  Все модели сначала предлагали ошибочные варианты. Верный итог Sol не
  сопровождается полным доказательством. Медианы: 3,477 / 5,959 / 4,666 с.
  Полные данные, комментарии и ограничения — в
  [README Дня 5](../../challenges/day-05-model-comparison/README.md).
- Веб-серии Дня 5 используют общий с CLI исполнитель. Текущий интерфейс
  проверен 2026-09-06 в браузере с подменой провайдера: три повтора, зачёт
  недопустимого набора, полные и неполные суммы, неизвестная оценка, остановка,
  новый запуск, сброс после обновления, отсутствие ключа и WebMCP-чтение.
  Детали и ограничения проверки — в
  [README Дня 5](../../challenges/day-05-model-comparison/README.md#проверки-и-ограничения).
- 2026-09-06 исправлена автоматическая оценка новых ответов Дня 5: распознаётся
  окончательный план после исправлений, многострочные поля, списки, стрелки
  и итоги вычислений, в том числе «Итоговый выбор» и исправление после
  объяснения с «поэтому». Два новых примера пользователя дают финальные
  28 и 29 баллов с переносами строк и без них. Все девять сохранённых ответов разбираются автоматически
  с совпадением ранее извлечённых финальных полей; ручная подстановка для
  сохранённой серии больше не используется. Комментарии к обоснованию сохранены.
- Неизвестные оценки отделены от провалов модели: интерфейс показывает «да»,
  «нет», «не оценено», распознанный блок и долю оптимальных среди оценённых.
- По уточнению пользователя от 2026-09-06 День 5 открывается без результатов;
  новые ответы живут в памяти вкладки. Загрузка архива и экспорт убраны,
  CLI выводит протокол в терминал без записи файлов. Правила времени жизни — в
  [спецификации](../features/day-05-model-comparison.md#веб-лаборатория-и-время-жизни-данных).
- Исправлен зачёт пользы недопустимых наборов и добавлена сумма за три повтора.
  Прошли 70 тестов Node.js с запрещённой записью файлов, интеграционный тест
  workerd, линтер, TypeScript и production-сборка. Промпт и API-параметры
  не изменялись, новых платных вызовов не было.
- Шестой челлендж реализован в `challenges/day-06-first-agent` как
  интерактивный Node.js CLI. Отдельный класс `Agent` инкапсулирует input/output
  policies, историю диалога, формирование запроса, вызов Responses API и
  нормализацию результата.
- Каждый успешный ход добавляется в локальную память агента, а следующий запрос
  передаёт модели всю переписку. Ошибки и непригодные ответы историю не меняют;
  `/reset` очищает её, `/exit` завершает CLI.
- Конфигурация и память принадлежат экземпляру, глобального состояния нет.
  Judge и массовый запуск намеренно не добавлены: они не нужны для критерия,
  но независимые экземпляры можно создавать обычным конструктором.
- Условия описаны в [задании 6](../features/day-06-first-agent.md), выбор CLI —
  в [решении 0006](../decisions/0006-day-06-cli-agent.md), теория и запуск — в
  [README Дня 6](../../challenges/day-06-first-agent/README.md).
- Для Дня 6 прошли 5 тестов без API-вызовов, синтаксическая проверка трёх
  исполняемых модулей и `npm audit` без известных уязвимостей. Реальный платный
  вызов не выполнялся.

## Следующие действия

1. По отдельному разрешению выполнить короткий реальный диалог Дня 6 и
   подтвердить передачу контекста через OpenAI API.
2. Запустить второе задание по его README, выполнить пару платных вызовов по
   решению пользователя и сравнить реальные ответы.
3. Перед публикацией проверить `git diff`, список новых файлов и отсутствие
   секретов; решение о коммите и публикации остаётся за пользователем.

Запись видео — на стороне пользователя, см.
[правило записи демонстраций](CONSTRAINTS.md#запись-демонстраций).
Дополнительные платные запуски третьего, четвёртого, пятого и шестого заданий
требуют отдельного решения пользователя.

## Блокеры и открытые вопросы

- Блокеров для первого челленджа нет.
- Реальные вызовы OpenAI и визуальная проверка streaming с реальной моделью
  для второго задания не выполнялись: отдельное разрешение на расходы не дано.
- Реальный вызов OpenAI для шестого задания не выполнялся: отдельное разрешение
  на расходы не дано; локальная логика проверена поддельным LLM-клиентом.
